# OpenHarness Enterprise LLM Configuration
# 内网大模型配置

# LLM Provider (使用 openai-compatible 以支持内网代理)
OH_PROVIDER=openai-compatible

# API Key (内网代理可能不需要真实 key，设置任意值即可)
OH_API_KEY=local-proxy

# API Base URL (内网模型代理地址)
OH_BASE_URL=http://10.1.19.246:39952/api/proxy/v1

# 默认模型
OH_MODEL=qwen3-235b-a22b-instruct-2507

# 超长任务
OH_MAX_TOKENS=65535